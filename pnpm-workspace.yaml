minimumReleaseAge: 0
confirmModulesPurge: false
allowBuilds:
  esbuild: true

# lefthook の postinstall（自動フック導入）は実行しない（無視を明示して警告も抑止）。
# フック配線は package.json の prepare（= lefthook install）で行う。
ignoredBuiltDependencies:
  - lefthook
